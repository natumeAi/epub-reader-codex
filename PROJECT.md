# EPUB Reader Project

## 项目目标

做一个自托管的 EPUB 阅读网页应用，主要供个人使用，最终部署到飞牛 NAS。

核心目标：

- 支持上传、管理和阅读 EPUB 电子书。
- UI 尽量接近 Apple Books 的观感与交互气质。
- 以手机竖屏为唯一主要使用场景，移动优先设计。
- 书架支持自由拖动排序。
- 书架支持将一本书拖到另一本书中间位置时创建文件夹，交互参考多看阅读。
- 文件夹内部也支持与书架一致的拖动排序。
- 不支持文件夹嵌套文件夹。
- 阅读器使用 epub.js，实现基础阅读、进度保存和后续阅读体验增强。
- 通过 Docker 部署到飞牛 NAS，目标硬件为 x86 J3455 CPU、8G 内存。

## 技术栈

前端：

- React
- Vite
- epub.js
- 移动优先 CSS

后端：

- Node.js
- Express
- SQLite

部署：

- Docker
- Docker Compose
- 飞牛 NAS 自托管部署

## UI 和产品约束

- 以竖屏窄视口 UI 为主要体验目标，不优先考虑桌面端、平板横屏或响应式大屏体验。
- 页面宽度、触摸区域、拖动交互、底部导航和阅读器控制都按竖屏窄视口设计。
- 支持手机、平板竖屏，以及桌面浏览器手动缩窄成竖屏窗口使用；在宽屏设备上内容区域应限制最大宽度并居中，保持与手机竖屏接近的观感，不扩展为桌面管理界面。
- 书架封面长宽保持固定，不随视口拉伸；书架根据可用宽度自动增减每行书籍数量，例如手机竖屏约 3 本一行，平板竖屏可显示更多列，宽屏仍限制最大内容宽度并居中。
- 书架界面只展示封面，不在封面下方显示书名、作者或其他文字信息；书籍识别主要依赖封面视觉。
- 风格参考 Apple Books：简洁、克制、轻量拟物感、重视封面展示和阅读沉浸感。
- 避免复杂后台管理界面，优先做个人使用时最顺手的核心流程。
- 所有拖动操作都要适合触摸屏使用。

## 书架与文件夹规则

- 书架根层使用统一排序列表，列表项可以是书籍或文件夹；文件夹在书架上像书籍一样参与拖动排序。
- 一本书同一时间只能处于一个位置：要么在书架根层，要么在某个文件夹内，不支持同一本书同时出现在多个文件夹。
- 拖动创建文件夹时，文件夹默认命名为“新建文件夹”，后续通过重命名功能修改。
- 拖动 A 到 B 的封面中间创建文件夹时，A 作为文件夹内第一本书，B 作为第二本书。
- 书架拖拽使用意图识别：靠近封面时优先锁定目标并避免目标提前被挤走；拖到书籍封面中心区才创建文件夹；拖到真实间隙区才显示排序挤开。
- 本阶段只支持“书籍拖到书籍中间创建新文件夹”；书籍拖入已有文件夹、文件夹拖到文件夹、文件夹拖到书籍中间都不触发创建或嵌套。
- 文件夹只允许包含书籍，不允许包含文件夹；文件夹嵌套必须在数据模型和接口层禁止。
- 空文件夹自动删除；该规则在实现“从文件夹移出书籍”时同步落地。
- 后续支持将根书架书籍拖入已有文件夹，并支持将文件夹内书籍拖回根书架；该能力必须和文件夹打开界面、文件夹内排序、从文件夹移出能力配套实现，避免误拖后无法恢复。

## 目录结构

计划目录结构：

```text
epub-reader/
  PROJECT.md
  README.md
  docker-compose.yml
  Dockerfile
  .dockerignore
  package.json
  server/
    package.json
    src/
      index.js
      app.js
      db/
        database.js
        migrations/
      routes/
        books.js
        folders.js
        reading.js
      services/
        epubService.js
        fileStorage.js
      middleware/
      utils/
    data/
      library.sqlite
      books/
      covers/
  client/
    package.json
    index.html
    vite.config.js
    src/
      main.jsx
      App.jsx
      routes/
      components/
        bookshelf/
        reader/
        folders/
        common/
      styles/
      api/
      hooks/
      state/
```

说明：

- `client/` 放 React + Vite 前端。
- `server/` 放 Express 后端、SQLite 数据库访问、EPUB 解析和文件存储逻辑。
- `server/data/books/` 存放 EPUB 文件；支持按钮上传，也支持手动复制 EPUB 到目录后自动入库，删除文件后自动移除对应书籍记录。
- `server/data/covers/` 存放提取或生成的封面图片。
- `server/data/library.sqlite` 是 SQLite 数据库文件。
- Docker 部署时需要把 `server/data/` 挂载为持久化目录。

## 编码约定

为了减少后续编码时的 token 消耗，并尽量避免跨对话失去方向、盲目堆代码、技术债累积等问题，开发时遵守以下规则。

### 全局方向

- 每个新对话先读 `PROJECT.md`，再决定下一步。
- 开始编码前先确认当前任务属于哪个阶段，不临时扩张目标。
- 只围绕当前清单项做最小必要改动，避免顺手重构无关模块。
- 遇到不确定需求时，先在 `PROJECT.md` 追加或修正约定，再写代码。
- 如果发现实现方向和项目目标冲突，优先停下来调整方案，而不是继续补丁式堆代码。

### Token 节省

- 回答和代码说明尽量短，只保留决策、变更点、验证结果。
- 不重复粘贴大段已有代码，优先引用文件路径和函数名。
- 修改文件前先用 `rg`、目录列表或局部读取定位上下文，避免整仓盲读。
- 单次任务只打开和修改必要文件，避免把无关文件带入上下文。
- 优先小步提交式开发：一个功能点、少量文件、可验证结果。
- 代码命名清晰，减少靠长注释解释逻辑。

### 实现原则

- 优先完成可运行的最小闭环，再逐步增强体验。
- 前后端接口先保持简单稳定，避免过早设计复杂抽象。
- 能用成熟库解决的核心问题不要手写，例如 EPUB 解析、拖拽排序、触摸交互基础能力。
- 数据模型必须服务书架、文件夹、排序和阅读进度四个核心场景。
- UI 组件按手机竖屏真实使用来拆分，不为桌面端提前增加复杂度。
- 文件夹只允许包含书籍，不允许包含文件夹。

### 技术债控制

- 每实现一个功能，必须同步更新“开发进度清单”。
- 临时方案必须在代码或 `PROJECT.md` 里标记原因和后续处理方式。
- 不在同一轮里同时做功能、重构、换技术方案，除非这是完成任务所必需。
- 重复代码出现第三次前再考虑抽象，避免为了“优雅”提前抽象。
- 数据库结构变更要通过迁移文件表达，不直接依赖手工改库。
- Docker 和 NAS 部署相关配置要保持简单，优先稳定运行和数据可恢复。

### 验证规则

- 每次改动后至少做一种验证：构建、测试、接口调用或页面运行检查。
- 书架拖拽、文件夹拖拽、阅读器交互必须用手机竖屏视口验证。
- 涉及数据写入时，要验证重启后数据仍然存在。
- 涉及 Docker 部署时，要验证持久化目录不会被镜像重建覆盖。

## 开发进度清单

- [x] 创建项目记忆文件 `PROJECT.md`
- [x] 补充编码约定和跨对话开发规则
- [x] 初始化项目目录结构
- [x] 初始化前端 React + Vite 项目
- [x] 初始化后端 Node.js + Express 项目
- [x] 配置 SQLite 数据库
- [x] 设计数据库表结构：书籍、文件夹、排序、阅读进度
- [x] 实现 EPUB 上传接口
- [x] 实现 EPUB 文件存储
- [x] 监控 EPUB 文件目录变化并同步添加或删除书籍
- [x] 实现 EPUB 元数据解析
- [x] 实现 EPUB 封面提取或生成
- [x] 实现书籍列表接口
- [x] 实现书籍删除接口
- [x] 实现书架首页 UI
- [x] 实现竖屏窄视口在手机、平板和桌面缩窄窗口下的观感一致性
- [x] 实现 Apple Books 风格移动端视觉基础
- [x] 实现书籍封面网格布局
- [x] 实现书架自由拖动排序
- [x] 实现拖动到两本书之间时自动挤开排序
- [x] 实现拖动到另一本书中间时创建文件夹
- [x] 实现通过拖动创建的文件夹在书架中像书籍一样拖动排序
- [x] 实现文件夹数据模型
- [ ] 实现文件夹打开界面
- [ ] 实现文件夹内书籍自由拖动排序（后端排序接口已完成，前端待接入）
- [x] 禁止文件夹嵌套文件夹
- [ ] 实现文件夹重命名（后端接口已完成，前端待接入）
- [ ] 实现从文件夹移出书籍
- [ ] 实现将根书架书籍拖入已有文件夹
- [ ] 实现将文件夹内书籍拖回根书架
- [ ] 实现空文件夹处理逻辑（删除文件导致空文件夹时后端已自动清理，移出书籍待实现）
- [ ] 接入 epub.js 阅读器
- [ ] 实现打开 EPUB 阅读
- [ ] 实现阅读器翻页动画：平移翻页和拟真翻页
- [ ] 实现阅读进度保存
- [ ] 实现继续阅读
- [ ] 实现章节目录
- [ ] 实现字体大小设置
- [ ] 实现页边距、行距、字距设置
- [ ] 实现主题设置
- [ ] 实现阅读器手机竖屏交互
- [ ] 实现 PWA 支持
- [ ] 实现 Dockerfile
- [ ] 实现 docker-compose.yml
- [ ] 配置数据持久化目录
- [ ] 在本地完成基础运行验证
- [ ] 编写 README 部署说明
- [ ] 针对飞牛 NAS 环境整理部署步骤

## 后续对话约定

每个新对话开始时，先阅读本文件，了解项目目标、技术约束、目录结构和开发进度，再继续实现功能。
